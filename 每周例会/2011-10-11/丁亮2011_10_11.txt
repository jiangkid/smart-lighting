上周和国庆节期间的所做工作：
	1.制定了GPRS通信规约大体上的三大部分，理清了思路；
	2.熟悉了SQL Server 2000的系统组成，包括创建数据库和表、数据的导入和导出等。
	3.再次回顾了服务器基于IOCO模型的创建。

本次开会的个人总结：
	1.昨天打算开始写服务器的代码，方向发生了错误，服务器这端不需要界面，它是属于客户端；
	服务器的这端只需要开启和停止服务器就足够了。
	
下周的工作计划：
	1.和吴红生一起商量服务器程序的大体架构；
	2.做到能从数据库里得到数据，并通过GPRS网络进行发送，而且能接收到GORS网络发送的数据，将它存入到数据库里；